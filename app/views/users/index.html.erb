<% @users.each do |user| %>
  <%= user.username %>
  <p><%= link_to 'Edit user', edit_user_path(user) %></p>
  <p><%= link_to 'Delete user', user_path(user), method: :delete, confirm: 'Are you sure you want to delete this user?' %></p>
<% end %>
